#include <bits/stdc++.h>
using namespace std;

int main() {

    // int count = 0;
    // while (count < 10) {
    //     cout << count << "\n";
    //     count += 1;
    // }

    // for (int count = 0; count < 10 ; count += 1) {
    //     cout << count << "\n";
    // }






    // int count1 = 0;
    // while (count1 < 10) {
    //     cout << count1 << "\n";
    //     count1 += 2;
    // }

    // for (_ ; _ ; _) {

    // }













    int count2 = 1;
    while (count2 < 100) {
        cout << count2 << "\n";
        count2 += count2;
    } 

    /*

    count *= 2 
    count = count * 2;
    */

    for (int count2 = 1; count2 < 100 ; count2 *= 2) {

    }

}